<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <permission
        android:name="com.example.dbstudent.READ_DATA"
        android:protectionLevel="normal" />s
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DBStudent">
        <activity
            android:name=".Activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.CreateStudentActivity">
        </activity>
        <activity
            android:name=".Activity.RemoveStudentActivity">
        </activity>
        <provider
            android:name=".ServiceProvider"
            android:authorities="com.example.dbstudent"
            android:exported="true"
            android:readPermission="com.example.dbstudent.READ_DATA" />
    </application>

</manifest>